{
  "name": "functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npm run build && npx @google-cloud/functions-framework --target=calendar --source=lib/index.js --port=5001",
    "emulate": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^4.9.0",
    "ics": "^2.41.0"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.2.0",
    "@types/dotenv": "^6.1.1",
    "dotenv": "^17.2.3",
    "typescript": "^5.1.6"
  }
}
