{
  "name": "functions",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/copy-shared-types.js",
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npm run build && npx firebase emulators:start --only functions,firestore",
    "emulate": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.6.0",
    "ics": "^3.8.1",
    "mailgun.js": "^12.1.1"
  },
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.2.0",
    "typescript": "^5.1.6"
  }
}
